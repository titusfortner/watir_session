require 'spec_helper'

describe WatirSession do
  it 'has a version number' do
    expect(WatirSession::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
